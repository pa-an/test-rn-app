## :arrow_forward: How to Run App

1. cd to the repo
2. Run `yarn`
3. cd ios && pod install && cd ..
4. Run Build for either OS
  * for iOS
    * run `yarn ios`
  * for Android
    * Run Genymotion
    * run `yarn android`
